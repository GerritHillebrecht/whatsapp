import { ShiningBorderDirective } from './shining-border.directive';

describe('ShiningBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new ShiningBorderDirective();
    expect(directive).toBeTruthy();
  });
});
